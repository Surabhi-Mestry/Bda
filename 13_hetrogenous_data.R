# Create a list containing the above vectors
my_list <- list(
  character_vector <- c("Alice", "Bob", "Charlie"),
  numeric_vector <- c(1, 2, 3, 4, 5),
  logical_vector <- c(TRUE, FALSE, TRUE)
)
# Print the list
print(my_list)
