# Create a data frame with employee details
employee_data <- data.frame(
  EmployeeID = c(1, 2, 3, 4, 5),
  FirstName = c("John", "Mary", "Robert", "Lisa", "Michael"),
  LastName = c("Smith", "Johnson", "Williams", "Davis", "Brown"),
  Age = c(30, 25, 35, 28, 32),
  Department = c("HR", "IT", "Finance", "Marketing", "Sales")
)

# Display the employee details
print(employee_data)
