# Create employee data
employee_data <- data.frame(
  EmployeeID = c(1, 2, 3, 4, 5),
  FirstName = c("John", "Jane", "Bob", "Alice", "Charlie"),
  LastName = c("Doe", "Smith", "Johnson", "Johnson", "Brown"),
  Salary = c(50000, 60000, 75000, 80000, 70000)
)

# Display the created data frame
print(employee_data)

